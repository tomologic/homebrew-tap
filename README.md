# Tomologic Homebrew Tap

Homebrew tap to easily provide osx users with wrench and future dev tools.

Requires that [Homebrew](http://brew.sh/) is installed.

## Usage

Install tools:

```
brew tap tomologic/homebrew-tap
brew install wrench
```

Upgrade tools:

```
brew update
brew upgrade wrench
```
